import { QueryResult, RowDataPacket } from "mysql2";
import { poolConnect } from "../utils/dbConnection.js";


export class BrandService {
    // tìm và trả về id của 1 brand thông
    async getIdByName(sname: string) {

    }
}