import express from 'express';

const router = express.Router();

/**
 * 
 */
router.get("/api/user-reports")

/**
 * 
 */
router.post("/api/user-reports/:reportId")

/**
 * 
 */
router.patch("/api/user-reports/:reportId")



export { router as userReportRoutes };